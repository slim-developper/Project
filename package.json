{
  "name": "project_v0",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"  cd server && npm run dev  \"       \"   cd client && npm start   \"  "
  },
  "author": "Slim_Developper",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.6.0",
    "concurrently": "^6.0.0",
    "react-bootstrap": "^1.5.1"
  }
}
